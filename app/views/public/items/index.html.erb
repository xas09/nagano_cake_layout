<p>商品一覧（全<%= @items.count %>件）</p>
<% @items.each do |item| %>
<div>
    <div>
        <div>
            <%= attachment_image_tag item, :image, :fill, 100, 100 %> 
        </div>
        <div>
            <%= item.name %>
        </div>
        <div>
            ¥<%= 1.1*item.price_without_tax %>
        </div>
    </div>
</div>
<% end %>
<!--管理者側で登録したデータを表示する。-->
<!--以下の内容が表示されること-->
<!--商品画像-->
<!--商品名-->
<!--税込価格-->
<!--ページネーション、左のジャンル検索機能は不要-->
